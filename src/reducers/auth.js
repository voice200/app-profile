const initialState = {
  username: null,
  accessToken: null,
  userId: null,
}

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
